fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# 얼굴 감정 분석 (facenet-pytorch/torch는 Dockerfile.base에서 --no-deps로 별도 설치)
tensorflow==2.20.0
moviepy==1.0.3
imageio
imageio-ffmpeg
Pillow
pandas

# 얼굴 랜드마크
mediapipe==0.10.33

# 이미지 처리
opencv-python-headless
numpy>=2.0.0

# GPT 판정
openai>=1.30.0

# 환경변수 로드
python-dotenv>=1.0.0
